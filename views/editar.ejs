<!DOCTYPE html>
<title>Carrito de compras</title>
<html lang="en">
    <%- include('partials/head') %>
<body>
    <%- include('partials/header') %>
   
            <h2>bebidas:</h2>

            <%for (let index = 0; index<plato.bebidas.length; index++) {%>
               
                <h4 class="title"><%=plato.bebidas[index].name%></h4>
               <form method="POST" action="/formularios/delete/<%=plato.bebidas[index].id%>?_method=DELETE">
            <button type="submit">eliminar</button>
            </form>
            <a href="/formularios/edit/<%=plato.bebidas[index].id%> " class="btn btn-primary">ver más</a>
                    
        <% } %> 
       
       <h2>postres:</h2>
            <%for (let index = 0; index<plato.postres.length; index++) {%>
               
                <h4 class="title"><%=plato.postres[index].name%></h4>
               <form method="POST" action="/formularios/delete/<%=plato.postres[index].id%>?_method=DELETE">
            <button type="submit">eliminar</button>
            </form>
            <a href="/formularios/edit/<%=plato.postres[index].id%>" class="btn btn-primary">ver más</a>
        <% } %> 
        <h2>hamburguesas:</h2>
            <%for (let index = 0; index<plato.hamburguesas.length; index++) {%>
               
                <h4 class="title"><%=plato.hamburguesas[index].name%></h4>
               <form method="POST" action="/formularios/delete/<%=plato.hamburguesas[index].id%>?_method=DELETE">
            <button type="submit">eliminar</button>
            </form>
            <a href="/formularios/edit/<%=plato.hamburguesas[index].id%>" class="btn btn-primary">ver más</a>
        <% } %> 
        <h2>pizzas:</h2>
            <%for (let index = 0; index<plato.pizzas.length; index++) {%>
               
                <h4 class="title"><%=plato.pizzas[index].name%></h4>
               <form method="POST" action="/formularios/delete/<%=plato.pizzas[index].id%>?_method=DELETE">
            <button type="submit">eliminar</button>
            </form>
            <a href="/formularios/edit/<%=plato.pizzas[index].id%> " class="btn btn-primary">ver más</a>
        <% } %> 

           
  
            <%- include('partials/footer') %>
</body>
</html>