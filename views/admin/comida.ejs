<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>

    <body>
      

            <main>
                <div class="container mt-5">
                    <h1><%= title %> </h1>
                    <div class="row">
                        <% comida.forEach(comida => { %>
                            <div class="col-12 col-md-6 col-lg-3 my-3">
                                <div class="card" style="width: 98%">
                                    <img src="/images/<%= comida.img %>"  class="card-img-top" alt="..." />
                                    <div class="card-body">
                                        <h5 class="card-title"><%= comida.name %> </h5>
                                        <a href="/comida/<%= comida.id %> " class="btn btn-primary">ver más</a>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
            </main>

            <%- include('../partials/footer') %>
    </body>

</html>